# Supplementary figure legends

**Supplementary Figure S1.** Micro-Fmax with input modality as rows and ontology as columns. F$_{max}$/S$_{min}$ favour graph-aware Hybrid/Hybrid_JK.

**Supplementary Figure S2.** Per-seed Micro-Fmax for all architectures and input modalities. F$_{max}$/S$_{min}$ favour graph-aware Hybrid/Hybrid_JK.

**Supplementary Figure S3.** Bar-and-error-bar rendering of the same Micro-Fmax data shown per-seed elsewhere; retained for readers who expect this encoding. F$_{max}$/S$_{min}$ favour graph-aware Hybrid/Hybrid_JK.

**Supplementary Figure S4.** Macro-Fmax with input modality as rows and ontology as columns. F$_{max}$/S$_{min}$ favour graph-aware Hybrid/Hybrid_JK.

**Supplementary Figure S5.** Per-seed Macro-Fmax for all architectures and input modalities. F$_{max}$/S$_{min}$ favour graph-aware Hybrid/Hybrid_JK.

**Supplementary Figure S6.** Bar-and-error-bar rendering of the same Macro-Fmax data shown per-seed elsewhere; retained for readers who expect this encoding. F$_{max}$/S$_{min}$ favour graph-aware Hybrid/Hybrid_JK.

**Supplementary Figure S7.** Micro-AUPRC with input modality as rows and ontology as columns. AUROC/AUPRC rank the models differently from F$_{max}$/S$_{min}$; see the paired metric-family figure. Macro-AUPRC values are provisional: archived tables were produced with a trapezoidal PR estimator that is upward-biased for tied scores (see docs/figure_data_integrity.md); regenerate before reporting.

**Supplementary Figure S8.** Per-seed Micro-AUPRC for all architectures and input modalities. AUROC/AUPRC rank the models differently from F$_{max}$/S$_{min}$; see the paired metric-family figure. Macro-AUPRC values are provisional: archived tables were produced with a trapezoidal PR estimator that is upward-biased for tied scores (see docs/figure_data_integrity.md); regenerate before reporting.

**Supplementary Figure S9.** Bar-and-error-bar rendering of the same Micro-AUPRC data shown per-seed elsewhere; retained for readers who expect this encoding. AUROC/AUPRC rank the models differently from F$_{max}$/S$_{min}$; see the paired metric-family figure. Macro-AUPRC values are provisional: archived tables were produced with a trapezoidal PR estimator that is upward-biased for tied scores (see docs/figure_data_integrity.md); regenerate before reporting.

**Supplementary Figure S10.** Macro-AUPRC with input modality as rows and ontology as columns. AUROC/AUPRC rank the models differently from F$_{max}$/S$_{min}$; see the paired metric-family figure. Macro-AUPRC values are provisional: archived tables were produced with a trapezoidal PR estimator that is upward-biased for tied scores (see docs/figure_data_integrity.md); regenerate before reporting. MLP under structure-only receives zeroed features and ignores graph edges, so it emits a constant score and acts as a null control, not a model. Its Macro-AUROC is 0.500 (chance); the ~0.70 Micro-AUROC reflects the term-frequency prior, not structure.

**Supplementary Figure S11.** Per-seed Macro-AUPRC for all architectures and input modalities. AUROC/AUPRC rank the models differently from F$_{max}$/S$_{min}$; see the paired metric-family figure. Macro-AUPRC values are provisional: archived tables were produced with a trapezoidal PR estimator that is upward-biased for tied scores (see docs/figure_data_integrity.md); regenerate before reporting. MLP under structure-only receives zeroed features and ignores graph edges, so it emits a constant score and acts as a null control, not a model. Its Macro-AUROC is 0.500 (chance); the ~0.70 Micro-AUROC reflects the term-frequency prior, not structure.

**Supplementary Figure S12.** Bar-and-error-bar rendering of the same Macro-AUPRC data shown per-seed elsewhere; retained for readers who expect this encoding. AUROC/AUPRC rank the models differently from F$_{max}$/S$_{min}$; see the paired metric-family figure. Macro-AUPRC values are provisional: archived tables were produced with a trapezoidal PR estimator that is upward-biased for tied scores (see docs/figure_data_integrity.md); regenerate before reporting. MLP under structure-only receives zeroed features and ignores graph edges, so it emits a constant score and acts as a null control, not a model. Its Macro-AUROC is 0.500 (chance); the ~0.70 Micro-AUROC reflects the term-frequency prior, not structure.

**Supplementary Figure S13.** Micro-AUROC with input modality as rows and ontology as columns. AUROC/AUPRC rank the models differently from F$_{max}$/S$_{min}$; see the paired metric-family figure. MLP under structure-only receives zeroed features and ignores graph edges, so it emits a constant score and acts as a null control, not a model. Its Macro-AUROC is 0.500 (chance); the ~0.70 Micro-AUROC reflects the term-frequency prior, not structure.

**Supplementary Figure S14.** Per-seed Micro-AUROC for all architectures and input modalities. AUROC/AUPRC rank the models differently from F$_{max}$/S$_{min}$; see the paired metric-family figure. MLP under structure-only receives zeroed features and ignores graph edges, so it emits a constant score and acts as a null control, not a model. Its Macro-AUROC is 0.500 (chance); the ~0.70 Micro-AUROC reflects the term-frequency prior, not structure.

**Supplementary Figure S15.** Bar-and-error-bar rendering of the same Micro-AUROC data shown per-seed elsewhere; retained for readers who expect this encoding. AUROC/AUPRC rank the models differently from F$_{max}$/S$_{min}$; see the paired metric-family figure. MLP under structure-only receives zeroed features and ignores graph edges, so it emits a constant score and acts as a null control, not a model. Its Macro-AUROC is 0.500 (chance); the ~0.70 Micro-AUROC reflects the term-frequency prior, not structure.

**Supplementary Figure S16.** Macro-AUROC with input modality as rows and ontology as columns. AUROC/AUPRC rank the models differently from F$_{max}$/S$_{min}$; see the paired metric-family figure. MLP under structure-only receives zeroed features and ignores graph edges, so it emits a constant score and acts as a null control, not a model. Its Macro-AUROC is 0.500 (chance); the ~0.70 Micro-AUROC reflects the term-frequency prior, not structure.

**Supplementary Figure S17.** Per-seed Macro-AUROC for all architectures and input modalities. AUROC/AUPRC rank the models differently from F$_{max}$/S$_{min}$; see the paired metric-family figure. MLP under structure-only receives zeroed features and ignores graph edges, so it emits a constant score and acts as a null control, not a model. Its Macro-AUROC is 0.500 (chance); the ~0.70 Micro-AUROC reflects the term-frequency prior, not structure.

**Supplementary Figure S18.** Bar-and-error-bar rendering of the same Macro-AUROC data shown per-seed elsewhere; retained for readers who expect this encoding. AUROC/AUPRC rank the models differently from F$_{max}$/S$_{min}$; see the paired metric-family figure. MLP under structure-only receives zeroed features and ignores graph edges, so it emits a constant score and acts as a null control, not a model. Its Macro-AUROC is 0.500 (chance); the ~0.70 Micro-AUROC reflects the term-frequency prior, not structure.

**Supplementary Figure S19.** Smin with input modality as rows and ontology as columns. F$_{max}$/S$_{min}$ favour graph-aware Hybrid/Hybrid_JK. Under structure-only, MLP/GAT/GCN reach exactly the predict-nothing S$_{min}$ (the test set's total IC), while Hybrid/Hybrid_JK saturate above the 0.99 threshold-scan bound and predict everything; both are degenerate, not tuned trade-offs.

**Supplementary Figure S20.** Per-seed Smin for all architectures and input modalities. F$_{max}$/S$_{min}$ favour graph-aware Hybrid/Hybrid_JK. Under structure-only, MLP/GAT/GCN reach exactly the predict-nothing S$_{min}$ (the test set's total IC), while Hybrid/Hybrid_JK saturate above the 0.99 threshold-scan bound and predict everything; both are degenerate, not tuned trade-offs.

**Supplementary Figure S21.** Bar-and-error-bar rendering of the same Smin data shown per-seed elsewhere; retained for readers who expect this encoding. F$_{max}$/S$_{min}$ favour graph-aware Hybrid/Hybrid_JK. Under structure-only, MLP/GAT/GCN reach exactly the predict-nothing S$_{min}$ (the test set's total IC), while Hybrid/Hybrid_JK saturate above the 0.99 threshold-scan bound and predict everything; both are degenerate, not tuned trade-offs.

**Supplementary Figure S22.** Mean Micro-F$_{max}$ per architecture and input modality as a quick-reference grid; colour scale clipped to the observed range.

**Supplementary Figure S23.** Macro-Fmax stratified by sequence homology bin, per input modality and ontology.

**Supplementary Figure S24.** Micro-AUPRC stratified by sequence homology bin, per input modality and ontology.

**Supplementary Figure S25.** Macro-AUPRC stratified by sequence homology bin, per input modality and ontology.

**Supplementary Figure S26.** Micro-AUROC stratified by sequence homology bin, per input modality and ontology.

**Supplementary Figure S27.** Macro-AUROC stratified by sequence homology bin, per input modality and ontology.

**Supplementary Figure S28.** Smin stratified by sequence homology bin, per input modality and ontology.

**Supplementary Figure S29.** Micro-Fmax stratified by information content bin, per input modality and ontology.

**Supplementary Figure S30.** Macro-Fmax stratified by information content bin, per input modality and ontology.

**Supplementary Figure S31.** Micro-AUPRC stratified by information content bin, per input modality and ontology.

**Supplementary Figure S32.** Macro-AUPRC stratified by information content bin, per input modality and ontology.

**Supplementary Figure S33.** Micro-AUROC stratified by information content bin, per input modality and ontology.

**Supplementary Figure S34.** Macro-AUROC stratified by information content bin, per input modality and ontology.

**Supplementary Figure S35.** Smin stratified by information content bin, per input modality and ontology.
